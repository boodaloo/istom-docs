# 🔑 Настройка аутентификации GitHub

## Проблема: "Authentication failed"

GitHub больше не принимает пароли для Git операций. Нужно использовать **Personal Access Token (PAT)**.

---

## ✅ Решение: Создайте Personal Access Token

### Шаг 1: Создайте токен на GitHub

1. Войдите на GitHub.com
2. Нажмите на ваш аватар справа вверху → **Settings**
3. Прокрутите вниз в левом меню → **Developer settings**
4. Выберите **Personal access tokens** → **Tokens (classic)**
5. Нажмите **Generate new token** → **Generate new token (classic)**

### Шаг 2: Настройте токен

**Note (описание):** `istom-docs access`

**Expiration (срок действия):** выберите срок (рекомендую **90 days** или **No expiration**)

**Select scopes (права доступа):** поставьте галочку на:
- ✅ **repo** (полный доступ к репозиториям)

Прокрутите вниз и нажмите **Generate token**

### Шаг 3: Скопируйте токен

⚠️ **ВАЖНО:** Токен показывается только один раз!

Скопируйте токен, он выглядит примерно так:
```
ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

**Сохраните его в надежном месте** (менеджер паролей, текстовый файл).

---

## 🚀 Использование токена

### Вариант 1: При push (рекомендуется для начала)

Когда делаете `git push`, Git попросит username и password:

```bash
git push -u origin main
```

**Username:** ваш GitHub username (в вашем случае: `boodaloo`)
**Password:** вставьте Personal Access Token (не ваш пароль!)

### Вариант 2: Сохранить токен в URL (один раз)

```bash
# Обновите remote URL с токеном:
git remote set-url origin https://ТОКЕН@github.com/boodaloo/istom-docs.git

# Теперь можно просто:
git push -u origin main
```

Замените `ТОКЕН` на ваш реальный токен.

**Пример:**
```bash
git remote set-url origin https://ghp_xxxxxxxxxxxxxxxxxxxx@github.com/boodaloo/istom-docs.git
```

### Вариант 3: Использовать Git Credential Manager (лучше всего)

**Для Linux/WSL:**

```bash
# Сохранить учетные данные в памяти на 1 час
git config --global credential.helper cache

# Или сохранить навсегда (в зашифрованном виде)
git config --global credential.helper store

# Теперь при первом push введите токен - он сохранится
git push -u origin main
```

При первом `git push` введите:
- Username: `boodaloo`
- Password: ваш Personal Access Token

После этого больше не будет спрашивать.

---

## 🔐 Альтернатива: SSH ключи (более безопасно)

### Если хотите использовать SSH вместо HTTPS:

**Шаг 1: Создайте SSH ключ**

```bash
# Создайте ключ (нажимайте Enter на все вопросы)
ssh-keygen -t ed25519 -C "your-email@example.com"

# Запустите SSH агент
eval "$(ssh-agent -s)"

# Добавьте ключ
ssh-add ~/.ssh/id_ed25519

# Покажите публичный ключ
cat ~/.ssh/id_ed25519.pub
```

**Шаг 2: Добавьте ключ на GitHub**

1. Скопируйте вывод команды `cat ~/.ssh/id_ed25519.pub`
2. На GitHub: Settings → SSH and GPG keys → New SSH key
3. Вставьте ключ и сохраните

**Шаг 3: Измените URL репозитория**

```bash
# Смените HTTPS на SSH
git remote set-url origin git@github.com:boodaloo/istom-docs.git

# Теперь можно пушить без токена
git push -u origin main
```

---

## ✅ Проверка настройки

```bash
# Проверьте текущий remote URL
git remote -v

# Должно быть либо:
# HTTPS: https://TOKEN@github.com/boodaloo/istom-docs.git
# или
# SSH: git@github.com:boodaloo/istom-docs.git
```

---

## 🔧 Быстрое решение прямо сейчас

**Самый простой способ для первого раза:**

1. Создайте Personal Access Token (инструкция выше)
2. Выполните:

```bash
cd /home/booda/istom/istom-docs

# При запросе пароля вставьте токен
git push -u origin main
```

3. Введите:
   - Username: `boodaloo`
   - Password: (вставьте ваш токен, начинающийся с `ghp_`)

4. Готово!

---

## 📱 Для сотрудников

Каждый сотрудник должен создать свой Personal Access Token:

1. Следовать инструкции выше
2. При клонировании репозитория использовать токен
3. Или настроить SSH ключи

**Или проще:** редактировать прямо на GitHub в браузере - авторизация уже есть!

---

## ❓ Частые вопросы

**Q: Где хранить токен?**
A: В менеджере паролей или в надежном файле. Не коммитьте его в Git!

**Q: Токен истек, что делать?**
A: Создайте новый токен на GitHub и обновите его локально.

**Q: Забыл токен, как восстановить?**
A: Нельзя восстановить. Создайте новый токен и удалите старый на GitHub.

**Q: Можно ли использовать один токен на всех компьютерах?**
A: Да, но лучше создать отдельные токены для каждого устройства (для безопасности).

---

## 🆘 Если всё еще не работает

1. Проверьте, что токен создан с правами **repo**
2. Проверьте, что токен не истек
3. Попробуйте создать новый токен
4. Убедитесь, что правильно скопировали токен (без пробелов)
5. Попробуйте SSH вместо HTTPS

---

**После настройки аутентификации вы сможете:**
- ✅ Пушить изменения на GitHub
- ✅ Работать совместно с командой
- ✅ Автоматически публиковать документацию
