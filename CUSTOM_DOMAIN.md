# 🌐 Настройка своего домена для базы знаний

## Зачем нужен свой домен?

Вместо `https://boodaloo.github.io/istom-docs/` клиенты будут заходить по адресу:
- `https://docs.istom.ru`
- `https://help.istom.ru`
- `https://support.istom.ru`

Это выглядит профессиональнее и проще запоминается.

---

## 📋 Пошаговая инструкция

### Шаг 1: Выберите поддомен

Рекомендуемые варианты:
- **docs.istom.ru** - для документации
- **help.istom.ru** - для помощи
- **kb.istom.ru** - knowledge base (база знаний)
- **support.istom.ru** - поддержка

---

### Шаг 2: Настройте DNS

Зайдите в панель управления вашего регистратора домена (где куплен домен istom.ru).

#### Для регистраторов РФ (Reg.ru, Timeweb, Beget, etc.):

1. Найдите раздел **"Управление DNS"** или **"DNS записи"**
2. Добавьте новую запись:

```
Тип записи: CNAME
Имя (Host): docs (или help, support)
Значение (Value): boodaloo.github.io
TTL: 3600 (или оставьте по умолчанию)
```

**Пример:**
```
docs.istom.ru → boodaloo.github.io
```

3. Сохраните изменения

#### Для Cloudflare:

1. Зайдите в панель Cloudflare
2. DNS → Add record
3. Заполните:
   - Type: `CNAME`
   - Name: `docs`
   - Target: `boodaloo.github.io`
   - Proxy status: можно включить (оранжевая иконка)
4. Save

---

### Шаг 3: Добавьте домен в GitHub Pages

1. Откройте настройки GitHub Pages:
   ```
   https://github.com/boodaloo/istom-docs/settings/pages
   ```

2. Найдите секцию **Custom domain**

3. Введите ваш поддомен:
   ```
   docs.istom.ru
   ```

4. Нажмите **Save**

5. GitHub начнет проверку DNS (может занять от нескольких минут до 24 часов)

6. Когда появится галочка ✅ - значит DNS настроен правильно

7. Поставьте галочку **Enforce HTTPS** (включит безопасное соединение)

---

### Шаг 4: Обновите mkdocs.yml (опционально)

Добавьте ваш домен в конфигурацию:

```yaml
site_url: https://docs.istom.ru
```

Это поможет для SEO и корректных ссылок.

Затем:
```bash
git add mkdocs.yml
git commit -m "Добавлен custom domain"
git push origin main
```

---

### Шаг 5: Готово!

Теперь клиенты могут заходить по адресу:
```
https://docs.istom.ru
```

---

## ⏱️ Сколько времени займет?

- **Настройка DNS:** 5 минут
- **Распространение DNS:** от 10 минут до 24 часов (обычно 1-2 часа)
- **Проверка GitHub:** несколько минут после распространения DNS
- **SSL сертификат:** автоматически от GitHub (бесплатно)

---

## 🔍 Проверка настройки DNS

Чтобы проверить, применились ли изменения DNS:

### В терминале:

```bash
# Проверка CNAME записи
dig docs.istom.ru CNAME

# Или с другой командой
nslookup docs.istom.ru
```

Должно вернуть: `boodaloo.github.io`

### Онлайн:

- https://dnschecker.org/
- Введите `docs.istom.ru`
- Выберите тип `CNAME`
- Проверьте в разных регионах

---

## ❓ Частые проблемы

### Проблема: "Domain's DNS record could not be retrieved"

**Решение:**
- Подождите еще - DNS может распространяться до 24 часов
- Проверьте правильность записи CNAME у регистратора
- Убедитесь что используете именно CNAME, а не A-запись

### Проблема: "HTTPS не работает"

**Решение:**
- Подождите после настройки DNS
- GitHub автоматически создаст SSL сертификат (может занять до часа)
- После появления сертификата включите "Enforce HTTPS"

### Проблема: "Сайт не открывается"

**Решение:**
- Проверьте что DNS запись создана правильно
- Очистите кэш браузера (Ctrl+Shift+Delete)
- Попробуйте в режиме инкогнито
- Проверьте на другом устройстве

---

## 🎯 После настройки

### Обновите ссылки на вашем сайте:

На главном сайте istom.ru добавьте ссылку:
```html
<a href="https://docs.istom.ru">База знаний</a>
```

### В email подписях:

```
База знаний: https://docs.istom.ru
```

### В программе iStom:

Добавьте кнопку "Справка" со ссылкой на документацию.

---

## 💡 Дополнительные возможности

### Редирект со старого адреса

Старый адрес `https://boodaloo.github.io/istom-docs/` будет автоматически перенаправлять на новый `https://docs.istom.ru` после настройки custom domain.

### Несколько доменов

Можно настроить несколько поддоменов на один сайт:
- `docs.istom.ru` (основной)
- `help.istom.ru` (алиас)
- `support.istom.ru` (алиас)

Но в GitHub Pages можно указать только один custom domain. Для алиасов используйте редирект на уровне DNS или веб-сервера.

---

## 📞 Нужна помощь?

Если возникли сложности с настройкой DNS:
1. Свяжитесь с техподдержкой вашего регистратора домена
2. Покажите им эту инструкцию
3. Попросите добавить CNAME запись: `docs.istom.ru → boodaloo.github.io`

---

## ✅ Чек-лист настройки

- [ ] Выбран поддомен (docs.istom.ru)
- [ ] Добавлена CNAME запись у регистратора
- [ ] Custom domain добавлен в GitHub Pages
- [ ] DNS проверка прошла успешно (зеленая галочка)
- [ ] Включен Enforce HTTPS
- [ ] Сайт открывается по новому адресу
- [ ] Обновлены ссылки на основном сайте

---

**После настройки все будет работать автоматически!**

При каждом обновлении документации (git push) сайт будет пересобираться и обновляться по вашему домену.
