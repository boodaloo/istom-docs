# 🚀 Инструкция: Настройка GitHub для совместной работы

## Шаг 1: Создайте GitHub аккаунт (если нет)

1. Перейдите на https://github.com
2. Нажмите "Sign up"
3. Создайте аккаунт (бесплатный план подходит)

---

## Шаг 2: Создайте новый репозиторий

1. Войдите в GitHub
2. Нажмите кнопку "+" справа вверху → "New repository"
3. Заполните форму:
   - **Repository name:** `istom-docs`
   - **Description:** "База знаний iStom"
   - **Visibility:** Private (или Public, если хотите открытый доступ)
   - ❌ НЕ ставьте галочку "Add a README file"
   - ❌ НЕ добавляйте .gitignore
   - ❌ НЕ добавляйте license
4. Нажмите "Create repository"

---

## Шаг 3: Загрузите код на GitHub

Выполните следующие команды в терминале:

```bash
# Перейдите в папку проекта
cd /home/booda/istom/istom-docs

# Настройте Git (если делаете первый раз)
git config --global user.name "Ваше Имя"
git config --global user.email "your-email@example.com"

# Переименуйте ветку в main
git branch -M main

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: База знаний iStom"

# Добавьте удаленный репозиторий (замените YOUR-USERNAME на ваш username)
git remote add origin https://github.com/YOUR-USERNAME/istom-docs.git

# Загрузите код на GitHub
git push -u origin main
```

**Примечание:** GitHub может попросить вас войти. Используйте Personal Access Token вместо пароля:
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите срок действия и права: `repo` (полный доступ к репозиториям)
4. Используйте этот токен вместо пароля

---

## Шаг 4: Включите GitHub Pages

1. Перейдите в ваш репозиторий на GitHub
2. Откройте **Settings** (вкладка справа вверху)
3. В левом меню найдите **Pages**
4. В разделе "Build and deployment":
   - **Source:** Deploy from a branch
   - **Branch:** выберите `gh-pages` и папку `/ (root)`
   - Нажмите **Save**

**Важно:** Ветка `gh-pages` создастся автоматически после первого запуска GitHub Actions (см. Шаг 5).

---

## Шаг 5: Запустите автоматическую публикацию

GitHub Actions уже настроен! При каждом `git push` будет:

1. Автоматически собираться сайт
2. Публиковаться на GitHub Pages

**Первый запуск:**

1. Перейдите в ваш репозиторий на GitHub
2. Откройте вкладку **Actions**
3. Если нужно подтвердить разрешение на запуск Actions - нажмите "I understand my workflows, go ahead and enable them"
4. Нажмите на workflow "Deploy Documentation"
5. Нажмите "Run workflow" → "Run workflow"

Или просто сделайте любое изменение и `git push` - Actions запустится автоматически.

**Проверка:**
- Перейдите во вкладку **Actions**
- Увидите зеленую галочку ✓ когда сборка завершится (1-2 минуты)

---

## Шаг 6: Найдите URL вашего сайта

После успешной публикации:

1. Перейдите в **Settings** → **Pages**
2. Увидите сообщение: "Your site is live at https://YOUR-USERNAME.github.io/istom-docs/"
3. Это и есть адрес вашей документации!

---

## Шаг 7: Добавьте сотрудников

### Добавление участников с правами редактирования:

1. Перейдите в **Settings** → **Collaborators** (или **Manage access**)
2. Нажмите **Add people**
3. Введите GitHub username или email сотрудника
4. Выберите роль:
   - **Write** - может редактировать и публиковать
   - **Maintain** - дополнительные права управления
   - **Admin** - полный доступ

5. Сотрудник получит приглашение на email
6. После принятия приглашения он сможет работать с репозиторием

### Что может делать сотрудник:

- Клонировать репозиторий: `git clone https://github.com/YOUR-USERNAME/istom-docs.git`
- Вносить изменения
- Публиковать изменения: `git push`
- Сразу видеть результат на сайте

---

## Шаг 8: Настройте свой домен (опционально)

Если хотите использовать свой домен (например, docs.istom.ru):

1. **Добавьте домен в GitHub:**
   - Settings → Pages → Custom domain
   - Введите `docs.istom.ru`
   - Нажмите Save

2. **Настройте DNS у вашего регистратора:**
   ```
   CNAME: docs.istom.ru -> YOUR-USERNAME.github.io
   ```

3. **Дождитесь проверки DNS** (может занять несколько часов)

4. **Включите HTTPS:**
   - В Settings → Pages поставьте галочку "Enforce HTTPS"

---

## 🔄 Ежедневная работа

### Для вас (администратора):

```bash
# 1. Обновить локальный код
git pull origin main

# 2. Внести изменения в файлы

# 3. Посмотреть что изменилось
git status

# 4. Добавить изменения
git add .

# 5. Сохранить изменения
git commit -m "Описание изменений"

# 6. Опубликовать
git push origin main

# 7. Через 1-2 минуты изменения появятся на сайте
```

### Для сотрудников:

**Вариант А: Через GitHub (в браузере)**
- Открыть файл на GitHub
- Нажать ✏️ (карандаш)
- Внести изменения
- Нажать "Commit changes"
- Готово!

**Вариант Б: На своем компьютере**
- Клонировать репозиторий (один раз)
- Делать изменения локально
- `git add` → `git commit` → `git push`

Подробная инструкция для сотрудников в файле **CONTRIBUTING.md**

---

## 📊 Мониторинг

### Проверка статуса сборки:

1. Откройте вкладку **Actions** на GitHub
2. Увидите историю всех сборок:
   - ✅ Зеленая галочка - успешно
   - ❌ Красный крестик - ошибка
3. Кликните на любую сборку чтобы увидеть детали

### Просмотр истории изменений:

1. Откройте вкладку **Code**
2. Нажмите на "commits" (например, "5 commits")
3. Увидите кто, когда и что изменил

---

## 🛠 Полезные команды Git

```bash
# Посмотреть статус
git status

# Посмотреть историю
git log --oneline

# Посмотреть изменения
git diff

# Отменить последний коммит (но сохранить изменения)
git reset HEAD~1

# Полностью откатить к предыдущей версии
git revert HEAD

# Создать новую ветку для экспериментов
git checkout -b feature/new-section

# Переключиться на main
git checkout main

# Слить ветку в main
git merge feature/new-section
```

---

## 🆘 Решение проблем

### Проблема: "Permission denied"

**Решение:**
1. Проверьте что вы добавлены как collaborator
2. Используйте Personal Access Token вместо пароля
3. Или настройте SSH ключи: https://docs.github.com/en/authentication/connecting-to-github-with-ssh

### Проблема: "failed to push some refs"

**Решение:**
```bash
git pull origin main --rebase
git push origin main
```

### Проблема: Actions не запускаются

**Решение:**
1. Settings → Actions → General
2. Убедитесь что "Allow all actions and reusable workflows" включено
3. Проверьте что есть файл `.github/workflows/deploy.yml`

### Проблема: Сайт не обновляется

**Решение:**
1. Проверьте Actions - нет ли ошибок
2. Подождите 2-3 минуты
3. Очистите кэш браузера (Ctrl+F5)
4. Проверьте Settings → Pages - правильно ли настроен source

---

## 📚 Дополнительные ресурсы

- **Git шпаргалка:** https://training.github.com/downloads/ru/github-git-cheat-sheet/
- **GitHub документация:** https://docs.github.com/
- **MkDocs Material:** https://squidfunk.github.io/mkdocs-material/
- **Markdown guide:** https://www.markdownguide.org/

---

## ✅ Чек-лист настройки

- [ ] Создан GitHub аккаунт
- [ ] Создан репозиторий istom-docs
- [ ] Код загружен на GitHub
- [ ] GitHub Pages включен
- [ ] GitHub Actions запущен
- [ ] Сайт доступен по URL
- [ ] Добавлены сотрудники как collaborators
- [ ] Сотрудники получили доступ к CONTRIBUTING.md
- [ ] Проверена автоматическая публикация (сделали изменение → увидели на сайте)

---

**Поздравляю! Совместная работа над базой знаний настроена! 🎉**

Теперь вы и ваши сотрудники можете работать вместе над документацией, и все изменения автоматически публикуются на сайте.
