# 🚀 Инструкция по публикации документации iStom

## 📦 Что готово к публикации

- **Папка:** `site/` - статический HTML сайт
- **Архив:** `istom-docs-site.tar.gz` (2.5 MB) - сжатая версия для передачи
- **Файлов:** 141 файл
- **Размер:** 8.3 MB (несжатый)

---

## 🌐 Варианты публикации

### Вариант 1: GitHub Pages (бесплатно, рекомендуется)

**Преимущества:**
- Бесплатный хостинг
- Автоматическое развертывание
- SSL сертификат включен
- Домен вида `username.github.io/istom-docs`

**Шаги:**

1. Создайте репозиторий на GitHub (например, `istom-docs`)

2. Инициализируйте Git и загрузите код:
```bash
cd /home/booda/istom/istom-docs
git init
git add .
git commit -m "Initial commit: iStom documentation"
git branch -M main
git remote add origin https://github.com/YOUR-USERNAME/istom-docs.git
git push -u origin main
```

3. Опубликуйте на GitHub Pages:
```bash
source venv/bin/activate
mkdocs gh-deploy
```

Готово! Сайт будет доступен по адресу:
`https://YOUR-USERNAME.github.io/istom-docs/`

---

### Вариант 2: Свой сервер (Apache/Nginx)

**Преимущества:**
- Полный контроль
- Можно использовать свой домен
- Нет зависимости от третьих сторон

**Шаги:**

1. Скопируйте содержимое папки `site/` на сервер:
```bash
# Через SCP
scp -r site/* user@your-server.com:/var/www/html/docs/

# Или через SFTP/FTP
# Загрузите содержимое папки site/ в корень сайта
```

2. Настройте веб-сервер:

**Nginx:**
```nginx
server {
    listen 80;
    server_name docs.istom.ru;

    root /var/www/html/docs;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}
```

**Apache (.htaccess):**
```apache
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.html [L]

# Gzip compression
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript
</IfModule>
```

3. Настройте SSL сертификат (Let's Encrypt):
```bash
sudo certbot --nginx -d docs.istom.ru
```

---

### Вариант 3: Netlify (бесплатно, быстро)

**Преимущества:**
- Очень простое развертывание
- Автоматический SSL
- CDN включен
- Бесплатный план достаточен

**Шаги:**

1. Зарегистрируйтесь на https://www.netlify.com

2. Перетащите папку `site/` в интерфейс Netlify
   (Drag & Drop)

3. Готово! Netlify даст вам адрес вида:
   `https://random-name.netlify.app`

4. (Опционально) Подключите свой домен в настройках

---

### Вариант 4: Vercel (бесплатно)

**Преимущества:**
- Мгновенное развертывание
- Отличная производительность
- Интеграция с Git

**Шаги:**

1. Установите Vercel CLI:
```bash
npm install -g vercel
```

2. Разверните:
```bash
cd /home/booda/istom/istom-docs
vercel --prod site/
```

3. Следуйте инструкциям в терминале

---

### Вариант 5: Cloudflare Pages (бесплатно)

**Преимущества:**
- Бесплатный безлимитный трафик
- Быстрая CDN
- Отличная производительность

**Шаги:**

1. Зарегистрируйтесь на https://pages.cloudflare.com

2. Подключите Git репозиторий или загрузите напрямую

3. Укажите папку `site/` как output directory

---

## 📝 После публикации

### Обновление документации

Когда нужно обновить документацию:

1. Отредактируйте файлы в папке `docs/`

2. Пересоберите сайт:
```bash
source venv/bin/activate
mkdocs build
```

3. Опубликуйте обновления:

**GitHub Pages:**
```bash
mkdocs gh-deploy
```

**Свой сервер:**
```bash
scp -r site/* user@server:/var/www/html/docs/
```

**Netlify/Vercel/Cloudflare:**
- Либо перетащите новую версию
- Либо настройте автоматическое развертывание из Git

---

## 🔧 Настройка своего домена

### После публикации на любой платформе:

1. **Купите домен** (например, docs.istom.ru)

2. **Настройте DNS записи:**

Для GitHub Pages:
```
CNAME: docs.istom.ru -> username.github.io
```

Для Netlify/Vercel/Cloudflare:
```
CNAME: docs.istom.ru -> your-app.netlify.app
```

Для своего сервера:
```
A: docs.istom.ru -> IP вашего сервера
```

3. **Укажите домен в настройках** платформы

4. **Включите SSL** (обычно автоматически)

---

## 📊 Проверка работы

После публикации проверьте:

- ✅ Главная страница загружается
- ✅ Навигация работает
- ✅ Поиск работает
- ✅ Изображения отображаются
- ✅ Ссылки не битые
- ✅ Мобильная версия работает
- ✅ SSL сертификат активен

---

## 🆘 Поддержка

Если возникли проблемы:

1. Проверьте логи сборки: `mkdocs build --verbose`
2. Проверьте права доступа к файлам
3. Убедитесь, что все файлы загрузились
4. Проверьте конфигурацию веб-сервера

---

## 💡 Рекомендации

**Для начала рекомендую:**
1. **GitHub Pages** - если у вас есть GitHub аккаунт
2. **Netlify** - если нужно максимально быстро и просто

**Для продакшена:**
- Свой сервер с SSL сертификатом
- Настройте резервное копирование
- Подключите мониторинг доступности

---

## 📞 Техническая информация

- **MkDocs версия:** 1.6.1
- **Material Theme версия:** 9.7.1
- **Python версия:** 3.14.2
- **Размер сайта:** 8.3 MB (141 файл)
- **Архив:** istom-docs-site.tar.gz (2.5 MB)

**Расположение файлов:**
- Исходники: `/home/booda/istom/istom-docs/docs/`
- Собранный сайт: `/home/booda/istom/istom-docs/site/`
- Архив: `/home/booda/istom/istom-docs/istom-docs-site.tar.gz`
