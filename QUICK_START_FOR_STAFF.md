# 🚀 Быстрый старт для сотрудников

## Как начать работать с базой знаний iStom

---

## 📋 Что вам нужно

Вы получили приглашение стать collaborator репозитория `istom-docs`. Это значит, что вы можете редактировать документацию!

---

## 🌐 Способ 1: Редактирование через браузер (рекомендуется для начала)

### Шаг 1: Примите приглашение

- Проверьте email от GitHub
- Перейдите по ссылке и нажмите **Accept invitation**

### Шаг 2: Откройте репозиторий

Перейдите на: https://github.com/boodaloo/istom-docs

### Шаг 3: Найдите файл для редактирования

1. Нажмите на папку `docs/`
2. Выберите нужный раздел, например:
   - `modules/schedule/` - расписание
   - `modules/patients/` - карта пациента
   - `modules/finance/` - финансы
   - `troubleshooting/` - решение проблем
3. Кликните на файл (например, `appointments.md`)

### Шаг 4: Редактируйте файл

1. Нажмите значок карандаша **✏️** ("Edit this file")
2. Внесите изменения
3. Используйте вкладку **Preview** для предпросмотра

### Шаг 5: Сохраните изменения

1. Прокрутите вниз
2. В поле "Commit message" напишите что изменили (например: "Добавил информацию о новой функции")
3. Нажмите **Commit changes**

### Шаг 6: Проверьте публикацию

- Зайдите во вкладку **Actions** - там идет автоматическая сборка
- Через 1-2 минуты изменения появятся на сайте документации

---

## 💻 Способ 2: Работа на своём компьютере

Если планируете много редактировать, лучше работать локально.

### Первоначальная настройка (один раз):

#### 1. Установите Git

**Windows:**
- Скачайте с https://git-scm.com/download/win
- Установите с настройками по умолчанию

**Mac:**
```bash
brew install git
```

**Linux:**
```bash
sudo apt install git
```

#### 2. Установите текстовый редактор

Рекомендую **VS Code**: https://code.visualstudio.com/

Или используйте любой другой (Notepad++, Sublime Text, и т.д.)

#### 3. Настройте Git

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "your-email@example.com"
```

#### 4. Создайте Personal Access Token

GitHub требует токен вместо пароля:

1. Перейдите на: https://github.com/settings/tokens
2. Нажмите **Generate new token** → **Generate new token (classic)**
3. Заполните:
   - **Note:** `istom-docs`
   - **Expiration:** 90 days
   - **Scopes:** поставьте ✅ **repo**
4. Нажмите **Generate token**
5. **СКОПИРУЙТЕ ТОКЕН** и сохраните в надёжном месте!

#### 5. Клонируйте репозиторий

```bash
# Клонируйте репозиторий на свой компьютер
git clone https://github.com/boodaloo/istom-docs.git

# Перейдите в папку
cd istom-docs
```

При запросе:
- **Username:** ваш GitHub username
- **Password:** вставьте токен (не пароль!)

#### 6. Установите Python и зависимости

**Windows:**
- Скачайте Python: https://www.python.org/downloads/
- При установке поставьте галочку "Add Python to PATH"

**Mac/Linux:**
```bash
# Python обычно уже установлен
python3 --version
```

Установите зависимости:
```bash
# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте его
# На Windows:
venv\Scripts\activate
# На Mac/Linux:
source venv/bin/activate

# Установите MkDocs
pip install -r requirements.txt
```

---

### Ежедневная работа:

#### 1. Обновите код перед началом работы

```bash
cd istom-docs
git pull origin main
```

#### 2. Запустите локальный сервер для предпросмотра

```bash
# Активируйте виртуальное окружение
source venv/bin/activate  # Mac/Linux
# или
venv\Scripts\activate  # Windows

# Запустите сервер
mkdocs serve
```

Откройте в браузере: http://127.0.0.1:8000

**Теперь все изменения сразу видны в браузере!**

#### 3. Редактируйте файлы

- Откройте файлы из папки `docs/` в вашем редакторе
- Сохраняйте изменения (Ctrl+S)
- Сразу видите результат в браузере (автообновление)

#### 4. Сохраните изменения в Git

```bash
# Посмотрите что изменилось
git status

# Добавьте изменённые файлы
git add .

# Создайте коммит с описанием
git commit -m "Описание ваших изменений"

# Отправьте на GitHub
git push origin main
```

При первом push введите токен вместо пароля.

#### 5. Готово!

Через 1-2 минуты изменения появятся на сайте.

---

## ✍️ Как писать в Markdown

### Заголовки
```markdown
# Заголовок 1 (главный)
## Заголовок 2 (раздел)
### Заголовок 3 (подраздел)
```

### Форматирование
```markdown
**Жирный текст**
*Курсив*
`код или команда`
```

### Списки
```markdown
- Пункт 1
- Пункт 2
  - Подпункт

1. Нумерованный
2. Список
```

### Ссылки
```markdown
[Текст ссылки](https://example.com)
[Ссылка на другую страницу](../patients/treatment-plan.md)
```

### Изображения
```markdown
![Описание](../images/screenshot.png)
```

Положите изображение в `docs/images/` и вставьте ссылку.

### Блоки предупреждений
```markdown
!!! tip "Совет"
    Полезная информация

!!! warning "Важно"
    Важная информация

!!! info "К сведению"
    Дополнительная информация
```

### Таблицы
```markdown
| Колонка 1 | Колонка 2 |
|-----------|-----------|
| Ячейка 1  | Ячейка 2  |
```

---

## 📋 Полезные команды Git

```bash
# Посмотреть статус
git status

# Посмотреть изменения
git diff

# Посмотреть историю
git log --oneline

# Отменить изменения в файле
git checkout -- filename.md

# Обновить код с GitHub
git pull origin main
```

---

## ❓ Частые вопросы

### Где находится документация?

Все файлы в папке `docs/`:
- `docs/modules/` - модули программы
- `docs/integrations/` - интеграции
- `docs/troubleshooting/` - решение проблем
- и т.д.

### Как добавить новую страницу?

1. Создайте новый `.md` файл в нужной папке
2. Напишите содержимое
3. Добавьте ссылку в `mkdocs.yml` в секцию `nav:`

### Изображение не отображается?

- Положите файл в `docs/images/`
- Путь: `![Описание](../images/filename.png)`
- Проверьте количество `../` (зависит от глубины папки)

### Изменения не появляются на сайте?

- Подождите 2-3 минуты
- Проверьте вкладку **Actions** на GitHub - нет ли ошибок
- Очистите кэш браузера (Ctrl+F5)

### Как откатить изменения?

```bash
# Посмотреть историю
git log

# Откатить последний коммит
git revert HEAD
git push origin main
```

### Ошибка при push?

Обновите код сначала:
```bash
git pull origin main
git push origin main
```

---

## 🆘 Нужна помощь?

1. Прочитайте полную инструкцию: **CONTRIBUTING.md**
2. Спросите у коллеги, который уже работает с документацией
3. Создайте Issue на GitHub с описанием проблемы
4. Напишите администратору репозитория

---

## ✨ Советы

- 💾 Делайте коммиты после каждого логического изменения
- 📝 Пишите понятные описания в commit message
- 🔄 Регулярно делайте `git pull` перед началом работы
- 👀 Проверяйте изменения локально перед push (`mkdocs serve`)
- 🤝 Обсуждайте большие изменения с командой

---

**Добро пожаловать в команду! Спасибо, что помогаете улучшать нашу базу знаний! 🎉**
